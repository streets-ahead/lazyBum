var LBBase = require('LBBase.js');

var Renderer = LBBase.extend(function() {
	Renderer.super_.apply(this, arguments);
});

Renderer.prototype.render = function(data, templateName) {
		
};

module.exports = Renderer;

