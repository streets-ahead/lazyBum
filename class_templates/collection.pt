var Collection = require('lazyBum').get('Collection');

var <%=objectName%> = Collection.extend(function() {
	<%=objectName%>.super_.apply(this, '<%=objectName%>', {
		//fill in schema here
	});
});

module.exports = <%=objectName%>;
