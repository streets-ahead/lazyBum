var Controller = require('lazyBum').get('Controller');

var that;
var model;

var <%=objectName%> = Controller.extend(function() {
	<%=objectName%>.super_.apply(this, arguments);
	that = this;
});

module.exports = <%=objectName%>;

<%=objectName%>.prototype.index_get = function(urlParts, query) {
	
};

<%=objectName%>.prototype.index_post = function(urlParts, query, postData) {
	
};


